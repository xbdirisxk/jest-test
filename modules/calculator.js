function add(x, y) {
	return x + y;
}

function sub(x, y) {
	return x - y;
}

let multiply = (x, y) => {
	return x * y;
};

let divide = (x, y) => {
	return x / y;
};

export { add, sub, multiply, divide };
